image: Ubuntu2004
stack: jdk 11

branches:
  only:
    - master

build: off

install:
  - java -jar ./artifacts/app-mbank.jar &
  - chmod +x gradlew
  - sleep 10  # Важно: добавляем задержку для запуска сервиса

build_script:
  - ./gradlew test --info